diff --git a/tui_gateway/methods_complete.py b/tui_gateway/methods_complete.py
index a7e5decc11..0752b3317c 100644
--- a/tui_gateway/methods_complete.py
+++ b/tui_gateway/methods_complete.py
@@ -221,6 +221,7 @@ def _(rid, params: dict) -> dict:
 
 
 @method("complete.slash")
+@_profile_scoped
 @_catch(5020)
 def _(rid, params: dict) -> dict:
     text = params.get("text", "")
diff --git a/tui_gateway/methods_tools.py b/tui_gateway/methods_tools.py
index 9d9e457f75..52dcf9d3e5 100644
--- a/tui_gateway/methods_tools.py
+++ b/tui_gateway/methods_tools.py
@@ -403,7 +403,7 @@ def _catalog_skills(cat: _Catalog, skills: dict[str, dict]) -> None:
         skills[k] = {"usage": usage(name), "origin": origin_of(name)}
 
 
-@_rpc("commands.catalog", 5020)
+@_scoped_rpc("commands.catalog", 5020)
 def _(rid, params: dict) -> dict:
     """Registry-backed slash metadata, categorized, no aliases. Discovery failures land in ``warning``
     (skills' message wins, then quick commands', then plugins')."""
@@ -811,7 +811,7 @@ _SLASH_BUILTINS = {
     "compress": _cmd_compress, "compact": _cmd_compress}
 
 
-@method("command.dispatch")
+@_scoped_rpc("command.dispatch", 5012)
 def _(rid, params: dict) -> dict:
     name, arg = _resolve_name(params.get("name", "").lstrip("/")), params.get("arg", "")
     session = _sessions.get(params.get("session_id", ""))
@@ -825,7 +825,7 @@ def _(rid, params: dict) -> dict:
     return _err(rid, 4018, f"not a quick/plugin/bundle/skill command: {name}")
 
 
-@method("slash.exec")
+@_scoped_rpc("slash.exec", 5030)
 def _(rid, params: dict) -> dict:
     session, err = _sess_nowait(params, rid)
     if err:
