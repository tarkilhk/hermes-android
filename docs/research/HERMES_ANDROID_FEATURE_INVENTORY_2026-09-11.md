# Hermes Android feature inventory

Source audit dated 2026-09-11. Android checkout `b8f1d981a272059076cefce179e2de9596dc9e06`. The initial `git status --short` was empty. Git printed a permission warning about the user's global ignore file. The package declares `2.1.1+2144` in [pubspec.yaml](../../pubspec.yaml#L1). This report describes the checked-out source, not an installed APK or a claim that every backend contract has been tested live today.

## The distinction that changes the comparison

The production entry point now opens **ProfileWorkspaceScreen** for every saved connection. It does not open the older `WorkspaceScreen` and `ChatScreen`. Notification taps also open the profile workspace. Those older screens still contain substantial implementations, tests and documentation, but they are not reachable through the normal current navigation graph. Count them as reusable work, not delivered mobile features. See [main.dart](../../lib/main.dart#L109), [connection navigation](../../lib/main.dart#L442), and the [profile workspace imports and navigation](../../lib/core/screens/profile_workspace_screen.dart#L1).

This is especially consequential for voice, Cron, Memory, Files, Settings, export/share, rich tool events, and the older Home/Activity/More shell. Their classes exist. The active profile workspace does not route to them. The old routes are in [WorkspaceScreen](../../lib/core/screens/workspace_screen.dart#L1188) and [SessionListScreen](../../lib/core/screens/session_list_screen.dart#L890), which themselves have no route from current `main.dart`.

In the tables, **Active** means implemented and reachable through the current app. **Partial** means a narrower active implementation or a backend dependency that matters. **Retained** means implemented in an older, currently unreachable screen. **Absent** means no implementation was found in the active application path. A slash command can provide an advanced alternative without constituting a dedicated mobile screen.

## Connect to Hermes and choose a profile

| Feature | Current state and evidence | Mobile assessment |
|---|---|---|
| Multiple saved hosts/connections | Active. List, add, edit and delete saved connections. Remembers the last connection and reopens it. [HomeScreen](../../lib/main.dart#L314), [connection actions](../../lib/main.dart#L484). | Essential for work/personal hosts and recovery when a host is offline. |
| Modern gateway setup | Active. User supplies label, host, port, dashboard credentials/proxy options and optional gateway URL. Saving probes profile discovery, a WebSocket handshake and session listing. The new form supplies an empty legacy API key. [setup validation](../../lib/main.dart#L741). | Essential. Setup should remain short while exposing advanced networking only when needed. |
| HTTPS, custom dashboard port and path prefix | Active through connection normalization and dashboard settings. [setup](../../lib/main.dart#L750), [DashboardClient](../../lib/core/services/connection_manager.dart#L1101). | Essential for private network and reverse proxy access. |
| Password dashboard and gateway ticket authentication | Active. Dashboard supports password login, session cookie/token handling and short-lived WebSocket tickets; ProfileGateway obtains those credentials. [dashboard authentication](../../lib/core/services/connection_manager.dart#L1133), [ticket credentials](../../lib/core/services/connection_manager.dart#L1216), [ProfileGateway](../../lib/core/services/profile_gateway.dart#L96). | Essential. The phone must authenticate to existing Hermes rather than become its administrator. |
| Proxy-authenticated dashboard | Active connection option. [setup](../../lib/main.dart#L921), [DashboardClient](../../lib/core/services/connection_manager.dart#L1082). | Useful for established private access deployments. |
| Legacy API-only gateway compatibility | Retained transports exist, but absent from active onboarding. ProfilesRepository explicitly rejects missing/unhealthy modern profile endpoints rather than falling back to unscoped requests. [ProfilesRepository](../../lib/core/services/profiles_repository.dart#L53), [onboarding](../../lib/main.dart#L741). | Compatibility policy needs to be explicit. Current minimum server requirements should be documented instead of promising automatic fallback. |
| Discover/select Hermes profiles | Active. Reads `profiles` and `profiles/active`, validates canonical names, renders profile buttons outside chat, and remembers selection per connection. [repository](../../lib/core/services/profiles_repository.dart#L82), [selection store](../../lib/core/services/profile_selection_store.dart#L10), [browser](../../lib/core/screens/profile_workspace_browser.dart#L659). | Essential for productive use of specialized agents without mixing their histories. |
| Switch profiles while work continues | Active. Each profile has separate resource and socket ownership. A foreground switch does not dispose the other profile's turns or drafts. [controller ownership](../../lib/core/services/profile_workspace_controller.dart#L181), [switch](../../lib/core/services/profile_workspace_controller.dart#L314). | Essential. Start work, leave it running and continue elsewhere. |
| Create, rename, clone, delete profiles | No dedicated active UI or profile CRUD methods in the discovery repository. Profile selection is not profile administration. [repository](../../lib/core/services/profiles_repository.dart#L58), [browser options](../../lib/core/screens/profile_workspace_browser.dart#L502). | Occasional mobile value; choose and inspect profiles before implementing full provisioning. |
| Backend install/start/stop/update, tunnels or service management | No dedicated active control found. The app connects to an existing dashboard/gateway. [onboarding](../../lib/main.dart#L741). | Low priority for normal productivity. A useful connection diagnosis and retry flow matters more. |

## Browse, organize and recover conversations

| Feature | Current state and evidence | Mobile assessment |
|---|---|---|
| Recent chats and project overview | Active. Profile home renders project rows, pinned chats and recents with title, age and status. [browser](../../lib/core/screens/profile_workspace_browser.dart#L183), [tree](../../lib/core/screens/profile_workspace_browser.dart#L334). | Essential. Fast return to an ongoing task is a core mobile workflow. |
| Server-backed conversation pagination | Active. REST pages of 50 with total/offset validation, automatic near-end load and explicit load-more/retry UI. [gateway](../../lib/core/services/profile_gateway.dart#L224), [browser](../../lib/core/screens/profile_workspace_browser.dart#L60). | Essential for established Hermes users with large histories. |
| Hide automated sessions | Active default excludes `cron`, `tool`, `subagent`, `kanban`; Include automated chats toggles all sources. Unknown sources stay visible. [visibility](../../lib/core/models/session_visibility.dart#L1), [browser menu](../../lib/core/screens/profile_workspace_browser.dart#L502). | High relevance. Keep human conversations usable while retaining access to agent runs. |
| Search chat history | Active server message/ID search and matching excerpts, merged with loaded local title matches. Includes archived chats. 350 ms debounce and up to 100 server matches. [browser search](../../lib/core/screens/profile_workspace_browser.dart#L40), [results](../../lib/core/screens/profile_workspace_browser.dart#L367), [gateway search](../../lib/core/services/profile_gateway.dart#L449). | Essential. This is already much more useful than searching loaded titles alone. |
| Search within a project / archive view | Partial. Local filtering of fetched project rows or loaded archive rows, not the same full server content search. Project collection is limited by the latest 5,000-session profile scan. [browser tree](../../lib/core/screens/profile_workspace_browser.dart#L398), [project scan](../../lib/core/services/profile_gateway.dart#L287). | High relevance. Narrowing search to a project should eventually use authoritative server search. |
| AI-assisted query rewrite and selectable search modes | Retained in old session search client/browser; absent from the active profile browser. [AI search service](../../lib/core/services/ai_search_query_rewriter.dart#L1), [active search](../../lib/core/services/profile_gateway.dart#L449). | Optional. Reliable ordinary search is the priority; natural language search can follow. |
| Rename, pin/unpin, mark read/unread | Active server mutations, via row menu and long press. [row actions](../../lib/core/screens/profile_row_actions.dart#L105), [update contract](../../lib/core/services/profile_gateway.dart#L493). | Essential. Read/unread is particularly useful for returning to work after interruptions. |
| Copy chat ID | Active row action. [row actions](../../lib/core/screens/profile_row_actions.dart#L121). | Useful for support and cross-client handoff; a human-friendly share link would be easier. |
| Archive/unarchive, browse archived chats | Active. Busy turns cannot be archived; archive view has server pagination. [row actions](../../lib/core/screens/profile_row_actions.dart#L128), [archive state](../../lib/core/services/profile_workspace_controller.dart#L816). | Essential. Keep the working list short without deleting history. |
| Delete conversation | Active, destructive confirmation and conservative checks against any live session with a matching durable ID. [confirmation](../../lib/core/screens/profile_row_actions.dart#L240), [gateway guard](../../lib/core/services/profile_gateway.dart#L564). | Useful, lower frequency than archive. Existing live-session restrictions can make deletion unavailable even for an idle but open session. |
| Move chat to another project | Active. Changes the remote working folder through `session.workspace.move`, not only a label. Blocks running and server-open sessions conservatively; offers projects with valid folders. [move UI](../../lib/core/screens/profile_row_actions.dart#L146), [gateway](../../lib/core/services/profile_gateway.dart#L519). | High relevance, with clear wording about changing the working folder. |
| Bulk selection, batch organization, ordered pins, undo | Absent active UI. Old More menu contains unavailable placeholders for batch/undo contracts. [More placeholders](../../lib/core/widgets/more_pane.dart#L133). | Useful after basic single-chat organization is reliable; small batches and undo are good mobile adaptations. |
| Cross-device conversation organization | Partial. Titles, pin/archive/unread and moves are server writes; answer-version grouping, drafts, scroll positions and other phone metadata are local. [session mutations](../../lib/core/services/profile_gateway.dart#L493), [answer groups](../../lib/core/services/profile_workspace_controller.dart#L283), [chat state](../../lib/core/services/profile_workspace_controller.dart#L70). | Essential to define which state follows the user and which belongs to the device. |
| Quick-chat retention/promotion | Retained old QuickChatStore has a 72-hour policy and promotion; current shortcut/share route simply creates an ordinary profile chat. [active intake](../../lib/core/screens/profile_workspace_screen.dart#L57), [old quick lifecycle](../../lib/core/screens/workspace_screen.dart#L486), [store](../../lib/core/services/quick_chat_store.dart#L1). | Quick capture is essential. Automatic expiry is a product choice, not something to claim the current entry point does. |

## Work inside a conversation

| Feature | Current state and evidence | Mobile assessment |
|---|---|---|
| Send and stream prompts | Active over profile-scoped gateway `prompt.submit`; handles delta, interim and completion events. [send](../../lib/core/services/profile_workspace_controller.dart#L1885), [events](../../lib/core/services/profile_workspace_controller.dart#L2023). | Essential. |
| Stop a running turn | Active Stop button and `session.interrupt`. [composer](../../lib/core/screens/profile_workspace_screen.dart#L532), [stop](../../lib/core/services/profile_workspace_controller.dart#L1960). | Essential when a phone user notices a mistake or excessive work. |
| Draft next prompt while a turn runs | Active in-memory draft, input hint changes to Draft your next message. Normal submit remains blocked while busy. [composer](../../lib/core/screens/profile_workspace_screen.dart#L455). | Essential for keeping momentum. Not a durable queued-send feature. |
| Send steering during a turn | Active `/steer` command invokes `session.steer`; ordinary busy send remains Stop. [commands](../../lib/core/services/profile_workspace_controller.dart#L1872). | High relevance. A discoverable steer action would be better on mobile than requiring command knowledge. |
| Background or side prompt | Active `/bg`, `/background`, `/btw`, plus completion text in command output. [commands](../../lib/core/services/profile_workspace_controller.dart#L1836), [events](../../lib/core/services/profile_workspace_controller.dart#L2045). | Useful for advanced multitasking; its progress deserves clear mobile status. |
| Paginated transcript | Active latest-first 50-message pages, older-page loading, compaction-aware history and fallback for a newly created session with no durable row yet. [gateway history](../../lib/core/services/profile_gateway.dart#L398). | Essential for long-running conversations. |
| Stable reading position and jump to latest | Active viewport anchor preservation, per-chat in-memory offset, Latest/New activity/Input needed affordances, reduced-animation support. [transcript](../../lib/core/screens/profile_transcript.dart#L25). | Essential. New tokens must not pull the reader away from earlier work. |
| Markdown, code, selection and copy | Active selectable prose, user and assistant copy buttons, fenced code rendering with existing code widget. [messages](../../lib/core/widgets/profile_message.dart#L54), [code widget](../../lib/core/widgets/markdown_code_block.dart#L1). | Essential for reading and reusing answers on a small screen. |
| Open web links | Active, explicit tap and external browser. Only HTTP/HTTPS without embedded credentials accepted. [message link validation](../../lib/core/widgets/profile_message.dart#L21). | Essential for research and generated output. |
| Inline generated images / rich artifacts | Partial. Markdown images render an Open image link button, not an inline fetched image. Remote host paths cannot be opened as phone files. [image builder](../../lib/core/widgets/profile_message.dart#L174). | High relevance. Generated images, PDFs and documents are useful mobile deliverables even if a general file browser is not. |
| Branch from an answer | Active for saved assistant answers; resolves durable message IDs/counts before branching. [answer actions](../../lib/core/screens/profile_workspace_screen.dart#L226), [controller](../../lib/core/services/profile_workspace_controller.dart#L1189), [branch count](../../lib/core/services/profile_gateway.dart#L345). | Useful for exploring alternatives while keeping the original. |
| Regenerate and answer-version navigation | Active per-answer regenerate and previous/next version controls. Grouping persisted locally; branches are actual server sessions. [answer actions](../../lib/core/screens/profile_workspace_screen.dart#L226), [version store](../../lib/core/services/profile_workspace_controller.dart#L1114). | High relevance. Version grouping should eventually be consistent across clients. |
| Edit user message and resend | Retained in older ChatScreen; no active edit action in ProfileMessage or active answer UI. A supported server `/undo` prefill may offer a command-based alternative, but is not edit-any-message UI. [old actions](../../lib/core/screens/chat_screen.dart#L3093), [command prefill](../../lib/core/services/profile_workspace_controller.dart#L1692). | High relevance for correcting phone typing and dictation. |
| Export/share conversation or individual answer | Retained old ChatScreen export via share; active message UI has copy only. Active `/save` stores on the Hermes host and reports the path. [old export](../../lib/core/screens/chat_screen.dart#L414), [active save](../../lib/core/services/profile_workspace_controller.dart#L1815), [message actions](../../lib/core/widgets/profile_message.dart#L54). | Essential for productivity outside Hermes. A server path is not a phone export. |
| Voice dictation, spoken replies, read aloud | Retained old ChatScreen/controller/services; absent in current profile composer. Slash catalog explicitly says phone voice is unavailable for composer-voice commands. [old voice](../../lib/core/screens/chat_screen.dart#L437), [active composer](../../lib/core/screens/profile_workspace_screen.dart#L382), [command availability](../../lib/core/models/slash_command.dart#L85). | High priority for mobile. This is a restoration/integration task, not a greenfield dependency. |
| Model/provider selection | Active searchable picker with per-chat model overrides, acknowledged model changes, persisted override and restore on resumed runtime. [picker call](../../lib/core/screens/profile_workspace_screen.dart#L574), [controller](../../lib/core/services/profile_workspace_controller.dart#L1448). | Essential. Work quality and latency depend on choosing suitable intelligence. |
| Reasoning effort | Active per-chat selection and restore through `session.config`; rejects unsupported local effort values. [intelligence write](../../lib/core/services/profile_workspace_controller.dart#L1497). | High relevance. Keep it alongside the model with understandable defaults. |
| Global/profile default model | Retained old SettingsScreen Set profile default. Active UI only changes chat intelligence; server commands may expose other configuration. [old settings](../../lib/core/screens/settings_screen.dart#L121), [active intelligence](../../lib/core/services/profile_workspace_controller.dart#L1497). | Occasional. Per-chat choice is more important on mobile. |
| Token/cost/context meter and reasoning display | No dedicated current meter or reasoning event rendering found. `/status` can show server text. Older ChatScreen has richer insight/reasoning handling. [active status command](../../lib/core/services/profile_workspace_controller.dart#L1826), [active event switch](../../lib/core/services/profile_workspace_controller.dart#L2023). | Useful for expensive/long runs; compact summaries beat an always-visible debug panel. |

## Attachments and mobile capture

| Feature | Current state and evidence | Mobile assessment |
|---|---|---|
| Attach phone files/images | Active single file picker per tap; repeated taps can add multiple drafts. Files show as removable name chips. [composer](../../lib/core/screens/profile_workspace_screen.dart#L417), [picker](../../lib/core/screens/profile_workspace_screen.dart#L483). | Essential. |
| Upload limits and sanitation | Active shared AttachmentDraftService. Up to 10 draft items, 64 MiB total, generic files up to 16 MiB each, sanitized images and sequential upload before submit. [draft service](../../lib/core/services/attachment_draft_service.dart#L290), [active integration](../../lib/core/services/profile_workspace_controller.dart#L1060), [submit coordinator](../../lib/core/services/profile_workspace_controller.dart#L1909). | Essential reliable intake; surface limits before the user invests in a draft. |
| Upload status/reorder/retry UI | Partial. Core service tracks upload state, but active composer only renders filename/removal chips. The older attachment tiles offered preview/reorder/individual retry. [active chips](../../lib/core/screens/profile_workspace_screen.dart#L417), [retained tile](../../lib/core/widgets/attachment_draft_tile.dart#L1). | High relevance, especially on intermittent mobile networks. |
| Camera capture and gallery multiselect | Retained old attachment picker and image_picker integration; active picker uses FilePicker without dedicated camera or gallery actions. [old attachment picker](../../lib/core/screens/chat_screen.dart#L935), [active picker](../../lib/core/screens/profile_workspace_screen.dart#L491). | High relevance. Camera is a mobile strength and should be easy to reach. |
| Receive text, links and files from Android Share | Active native SEND/SEND_MULTIPLE plus intent services; creates a chat, stages shared text and files, requires explicit send. [manifest](../../android/app/src/main/AndroidManifest.xml#L40), [main routing](../../lib/main.dart#L392), [staging](../../lib/core/screens/profile_workspace_screen.dart#L57). | Essential. Capture from browser, mail, screenshots and documents is one of the best reasons to use a phone client. |
| Share review/destination sheet | Retained explicit review sheet in old WorkspaceScreen; active intake stages directly in a new chat under the current remembered profile, without a dedicated project/profile destination review. [active intake](../../lib/core/screens/profile_workspace_screen.dart#L57), [old review path](../../lib/core/screens/workspace_screen.dart#L510). | High relevance. Keep deliberate send, add an easy destination choice rather than forcing later filing. |
| App launcher Quick chat shortcut | Active manifest action and launch-intent routing to a new profile chat. [manifest](../../android/app/src/main/AndroidManifest.xml#L36), [main](../../lib/main.dart#L400), [intake](../../lib/core/screens/profile_workspace_screen.dart#L57). | High relevance for fast capture. |
| Remote file browser | Retained FilesScreen provides directory listing, text preview, download and Add to chat reference. Not reachable from active workspace. [FilesScreen](../../lib/core/screens/files_screen.dart#L42), [file actions](../../lib/core/screens/files_screen.dart#L219). | Conditional. General host filesystem navigation is low priority; selecting a project document or retrieving a generated deliverable has high mobile value. Do not discard those narrower workflows with the whole file-browser concept. |
| Remote file editing, terminal, diff/review workbench | No active native implementation found. Text file preview/download exists only in retained FilesScreen. [file screen](../../lib/core/screens/files_screen.dart#L86), [active workspace](../../lib/core/screens/profile_workspace_screen.dart#L1). | General editing/workbench is low priority. Readable change summaries and approval are relevant, full IDE parity is not. |

## Projects and work context

| Feature | Current state and evidence | Mobile assessment |
|---|---|---|
| Discover server projects and chats | Active `projects.tree` and `projects.project_sessions`; current list flattens repos/groups to unique chats. [gateway projects](../../lib/core/services/profile_gateway.dart#L267). | Essential, shared server organization should be the default. |
| Create project | Active name dialog then absolute Hermes-host folder dialog, passed to `projects.create`. [dialog](../../lib/core/screens/profile_workspace_screen.dart#L721), [gateway](../../lib/core/services/profile_gateway.dart#L480). | Useful, but absolute path entry is awkward on a phone. Prefer selecting a known folder or creating a project from a chat. |
| New chat in a project | Active project row button and createChat with remote working directory. [project row](../../lib/core/screens/profile_workspace_browser.dart#L183), [controller](../../lib/core/services/profile_workspace_controller.dart#L655). | Essential. |
| Project label in conversation header | Active async project lookup and clear connection/project context. [header](../../lib/core/screens/profile_workspace_screen.dart#L142), [lookup](../../lib/core/services/profile_workspace_controller.dart#L770). | Essential to avoid sending instructions in the wrong context. |
| Rename/archive/delete project | Retained ProjectDetailScreen implements these controls; active browser only opens projects and creates chats/projects. [retained detail](../../lib/core/widgets/project_detail_screen.dart#L212), [active project UI](../../lib/core/screens/profile_workspace_browser.dart#L183). | Useful but less frequent than chat filing. |
| Multiple folders/repos/worktrees in a project | Backend tree carries them, but active create UI accepts one path and chat list flattens hierarchy. Retained project detail includes repo/tree representations. [gateway flattening](../../lib/core/services/profile_gateway.dart#L287), [old repo detail](../../lib/core/widgets/project_detail_screen.dart#L944). | Conditional. Expose the working context when choosing where a task runs; avoid duplicating desktop repository navigation. |
| Local Spaces and migration to projects | Retained services/screens and migration preview/write path, not routed through active browser. [Spaces](../../lib/core/screens/spaces_screen.dart#L1), [migration](../../lib/core/widgets/space_migration_preview.dart#L1), [old workspace initialization](../../lib/core/screens/workspace_screen.dart#L605). | Migration may be required for existing users, but a second permanent organization system would add confusion. |
| Project instructions, resources and asset library | No dedicated active UI. Old More Assets entry explicitly unavailable pending server index. [More](../../lib/core/widgets/more_pane.dart#L122), [active project UI](../../lib/core/screens/profile_workspace_browser.dart#L183). | High value if scoped to useful project reference material and generated results. |

## Oversee running agents and act on requests

| Feature | Current state and evidence | Mobile assessment |
|---|---|---|
| Activity across visited profiles | Active menu opens a list of application-owned chats with non-idle status, including completed/failed entries. Links back to original profile/chat. This is not a complete server-wide durable activity feed. [activity getter](../../lib/core/services/profile_workspace_controller.dart#L259), [activity UI](../../lib/core/screens/profile_workspace_browser.dart#L334). | Essential. Broaden it into a dependable Needs attention/Running/Completed inbox. |
| Attention-ranked Home digest and rich Activity filters | Retained HomePane/ActivityPane/journal implementation in older shell. Active profile home is projects/pins/recents. [retained Home](../../lib/core/widgets/home_pane.dart#L1), [retained Activity](../../lib/core/widgets/activity_pane.dart#L1), [active tree](../../lib/core/screens/profile_workspace_browser.dart#L334). | High relevance. This may be more valuable than many desktop management pages. |
| Approval request | Active inline command/description with Deny and Allow once. Rehydrated on resume. [approval UI](../../lib/core/screens/profile_workspace_screen.dart#L345), [reply](../../lib/core/services/profile_workspace_controller.dart#L1964). | Essential. This is a primary mobile supervision job. |
| Session-wide/always approval policies | No active option beyond once/deny; controller explicitly rejects other choices. [approval validation](../../lib/core/services/profile_workspace_controller.dart#L1964). | Optional and sensitive to server semantics. Clear one-time decisions suffice for initial mobile flow. |
| Clarification questions | Active inline choice/freeform widget, sequential multiple-question batches, saved answer locks, expiration checks and reconnect handling. [question UI](../../lib/core/screens/profile_workspace_screen.dart#L205), [controller](../../lib/core/services/profile_workspace_controller.dart#L1977), [pending question](../../lib/core/services/profile_workspace_controller.dart#L111). | Essential. Make the phone a reliable way to unblock the agent. |
| Sudo/password/secret request | Retained rich dialogs and handlers in old ChatScreen; active ProfileChat event switch does not handle those event families. [old handlers](../../lib/core/screens/chat_screen.dart#L2224), [active events](../../lib/core/services/profile_workspace_controller.dart#L2023). | Relevant when a legitimate task blocks. Requires explicit masked-entry handling and expiry, not generic chat text. |
| Tool activity | Partial active support: live Using tool label and collapsible persisted tool results. It does not render all rich live activity metadata handled by old ChatScreen. [live tool](../../lib/core/screens/profile_workspace_screen.dart#L316), [tool groups](../../lib/core/widgets/profile_tool_activity.dart#L1). | Essential concise status; expandable details help diagnose stuck work. |
| Agent notifications, reviews, subagent status, rich reasoning events | Retained models/widgets/ChatScreen event integration. Active switch handles text, tool start/complete, background result, approval, clarify and end/error only. [active events](../../lib/core/services/profile_workspace_controller.dart#L2023), [retained insights](../../lib/core/widgets/gateway_insight_card.dart#L1). | Reviews and blocked subagents are relevant; noisy intermediate reasoning should be optional. |
| Completion/attention Android notifications | Active local notifications with original host/profile/session payload, manual enable permission action and click routing. Suppressed while the same chat is visible; can notify while another chat/profile is in foreground. [main wiring](../../lib/main.dart#L151), [suppression](../../lib/core/services/profile_workspace_controller.dart#L2136). | Essential. Notification taps must always open the exact work item, which current ownership checks protect. |
| Push while process is dead / notification actions | No push provider, foreground service or background scheduler found in the active native manifest. Local notifications require app code to observe events; reconnection may surface results later. Notification tap exists, direct reply/approve actions do not. [manifest](../../android/app/src/main/AndroidManifest.xml#L1), [notification setup](../../lib/main.dart#L151). | High priority for dependable mobile supervision. Never promise continuous push based only on a WebSocket and local notification plugin. |

## Automation, skills, memory and settings

| Feature | Current state and evidence | Mobile assessment |
|---|---|---|
| Cron list/create/edit/pause/resume/trigger/delete | Retained CronScreen implements all of these through dashboard APIs. Form has name, prompt, schedule, script-only switch. Not reachable from active profile workspace. [CronScreen](../../lib/core/screens/cron_screen.dart#L47), [job form](../../lib/core/screens/cron_screen.dart#L270). | Relevant. Start with review failures, run now and pause/resume; provide a simple scheduler for common recurring work. |
| Cron run history and results | Retained screen displays job metadata such as status/run times, but no complete active native job/history/results workflow. [retained job rows](../../lib/core/screens/cron_screen.dart#L434). | High relevance to productivity. Completion without a readable deliverable is not enough. |
| Skills browsing | Active `/skills` and slash suggestions from server catalog. Dedicated retained SkillsScreen only lists name/description. [active skills command](../../lib/core/services/profile_workspace_controller.dart#L1855), [old list](../../lib/core/screens/skills_screen.dart#L40). | Essential discovery and invocation; a full package manager is less urgent. |
| Invoke skills, quick commands, plugins/bundles | Active server-owned catalog, alias support, argument completion and dispatch; handles send/prefill/exec results. [catalog](../../lib/core/services/profile_workspace_controller.dart#L1585), [dispatch](../../lib/core/services/profile_workspace_controller.dart#L1616), [model](../../lib/core/models/slash_command.dart#L18). | High relevance. Mobile should expose common actions without forcing users to memorize commands. |
| Install/update/remove skill packages, edit skill source | No dedicated active UI. Catalog/command support is not proof of complete skill lifecycle management. [active command contract](../../lib/core/services/profile_workspace_controller.dart#L1616). | Occasional use. Browse available capability and invoke it before adding authoring/package administration. |
| View memory | Retained read-only MemoryScreen tries memory endpoint then config fallback. No current route. [MemoryScreen](../../lib/core/screens/memory_screen.dart#L48). | Relevant to understand what Hermes remembers. |
| Edit/delete memory and persona/system instructions | No dedicated active editor found. Generic server slash dispatch may offer commands when advertised, but no guaranteed native workflow. [memory screen](../../lib/core/screens/memory_screen.dart#L110), [active dispatch](../../lib/core/services/profile_workspace_controller.dart#L1616). | Relevant focused controls for correcting durable information; full config-file editing has lower value. |
| Provider credentials, MCP/tool settings, gateway configuration | No dedicated active native settings page. Old SettingsScreen covers default model, device preferences and connection metadata, not a full backend administration console. [SettingsScreen](../../lib/core/screens/settings_screen.dart#L196). | Selectively relevant: health, authentication failure and enabling a needed capability. Full desktop configuration parity is unnecessary. |
| Dashboard escape hatch | Retained More entry and old WorkspaceScreen external dashboard launch; absent in active browser menu. [More dashboard](../../lib/core/widgets/more_pane.dart#L193), [launcher](../../lib/core/screens/workspace_screen.dart#L733). | Useful for infrequent administrative work and a practical interim bridge. |
| Accent color | Active per-device picker with persisted choice. Profile/project colors and status remain distinct. [picker](../../lib/core/screens/profile_workspace_screen.dart#L618). | Useful personalization, lower priority than core workflows. |
| Dark/light/system theme and text size | Runtime honors stored settings and system scaling. Controls remain in unreachable SettingsScreen. [app theme](../../lib/main.dart#L197), [retained controls](../../lib/core/screens/settings_screen.dart#L321). | Accessibility controls should be reachable. These are integration gaps, not missing theme infrastructure. |
| Voice choice and verbose display preferences | Retained settings, not active workspace controls. [settings](../../lib/core/screens/settings_screen.dart#L338). | Voice choice useful after voice returns; verbose logs belong behind a disclosure. |
| Encrypted configuration backup export | Retained SettingsScreen export uses PBKDF2 and AES-GCM. No active export route found. [export](../../lib/core/screens/settings_screen.dart#L373), [envelope](../../lib/core/services/config_backup.dart#L193). | Useful for device replacement; not a reason to postpone core chat work. |
| Restore configuration | Active from connection picker, including empty state, with merge/replace backed by existing backup service. [restore](../../lib/main.dart#L330), [empty state](../../lib/main.dart#L600). | Useful, particularly at onboarding. |
| Backup scope | Partial, curated preference allowlist. Theme/voice/text size, search/model/source filters/Spaces included. New profile selection, accent and answer version state are not explicitly included in the allowlist. [scope](../../lib/core/services/config_backup_service.dart#L45). | Be precise: this is configuration restore, not full chat/device state backup. |

## Reliability, privacy and backend dependencies

The current application has a strong ownership model. A chat key includes saved connection ID, an authenticated connection identity, profile and durable session ID. ProfileGateway puts `profile` in REST/RPC requests and validates owners in responses. Credential changes create a new controller identity instead of retargeting an existing socket or pending notification. [chat keys](../../lib/core/services/profile_workspace_controller.dart#L21), [scoped requests](../../lib/core/services/profile_gateway.dart#L205), [registry](../../lib/core/services/profile_workspace_registry.dart#L15), [identity](../../lib/core/services/profile_connection_identity.dart#L10).

Credentials live in platform secure storage, while connection metadata lives in shared preferences. Legacy secret migration includes failure handling. Android system backup is disabled; cleartext traffic remains permitted to support HTTP/private LAN hosts. This is configurable HTTP/HTTPS access, not enforced transport encryption. [credential storage](../../lib/core/services/connection_manager.dart#L21), [migration](../../lib/core/services/connection_manager.dart#L170), [manifest](../../android/app/src/main/AndroidManifest.xml#L6).

The active recovery scheme persists pending chat **identities**, not prompts, transcript text or paths. It reconnects with bounded backoff, resumes the server session, reads server history and does not resend uncertain prompts. Process restart restores pending owners where available. Unsent drafts, transcript cache, completed activity and scroll offsets are chiefly in memory; this is not a full offline client or durable outbox. [reconnect](../../lib/core/services/profile_workspace_controller.dart#L2144), [journal](../../lib/core/services/profile_workspace_controller.dart#L2241), [draft state](../../lib/core/services/profile_workspace_controller.dart#L70), [uncertain submit](../../lib/core/services/profile_workspace_controller.dart#L1947).

The older recovery coordinator/turn journal implements a separate advertised durable contract with legacy fallback. Its presence and README description must not be used to assert that the new profile route has exactly-once submission/recovery. Current profile code explicitly uses conservative no-resend and server reconciliation. [old recovery](../../lib/core/services/gateway_turn_recovery.dart#L1), [active prompt](../../lib/core/services/profile_workspace_controller.dart#L1885).

The active client depends on a modern dashboard with profiles and paginated session/history REST, a working authenticated desktop gateway, ownership metadata, projects RPCs for project features, search endpoint for content search, and command catalog/dispatch for commands. Its profile repository has no legacy fallback. The client checks safety-sensitive ownership, but comments document limits in stock server live-session contracts and a profile deletion race. Do not reconnect old management screens without adding equivalent profile scoping. [discovery](../../lib/core/services/profiles_repository.dart#L53), [gateway contract](../../lib/core/services/profile_gateway.dart#L205), [move safety](../../lib/core/services/profile_gateway.dart#L519).

No native offline transcript store/outbox, biometric app lock, certificate-pinning UI, push messaging service, notification inline reply, Android widget, hardware-keyboard command palette, native kanban board or complete audit/log console was found in the active route. These are source-audit observations rather than proof that no optional backend command can achieve part of the same outcome. The complete active UI is concentrated in [ProfileWorkspaceScreen](../../lib/core/screens/profile_workspace_screen.dart#L22), [ProfileWorkspaceBrowser](../../lib/core/screens/profile_workspace_browser.dart#L17), [ProfileMessage](../../lib/core/widgets/profile_message.dart#L12), [main.dart](../../lib/main.dart#L1) and [AndroidManifest.xml](../../android/app/src/main/AndroidManifest.xml#L1).

## Documentation discrepancies and planning implications

1. README identifies v2.1.0/build 2140 while package source is v2.1.1/build 2144. More seriously, it presents the former Workspace/More navigation, native voice, rich event handling, exports and 72-hour quick-chat lifecycle as current capabilities. Current entry-point routing does not expose those implementations. [README](../../README.md#L1), [pubspec](../../pubspec.yaml#L1), [current route](../../lib/main.dart#L442).
2. README's on-device/full-text/AI search mode description belongs to the former browser. The active browser uses server content/ID search plus loaded title matches and exposes its 100-result cap. [README](../../README.md#L73), [active search](../../lib/core/screens/profile_workspace_browser.dart#L367).
3. Claims of multi-attachment support are broadly true at the draft/service level, but active acquisition is one FilePicker result per invocation and active draft UI lacks the older thumbnails/reorder/retry controls. [active picker](../../lib/core/screens/profile_workspace_screen.dart#L491), [service limits](../../lib/core/services/attachment_draft_service.dart#L290).
4. Runtime theme/text-size support survives the navigation change, but the settings controls do not. Restore survives through HomeScreen; export remains behind the old SettingsScreen. [main theme](../../lib/main.dart#L197), [restore](../../lib/main.dart#L330), [old settings](../../lib/core/screens/settings_screen.dart#L321).
5. Active profile switching, ownership checks, answer branches/versions, paginated history, shared server organization and command dispatch are substantial delivered improvements that older high-level docs can understate. Preserve those while integrating useful retained features, rather than restoring old screens wholesale.

For a mobile backlog, the strongest near-term work is to connect existing useful implementations to the profile-owned architecture: voice/dictation and camera capture, output export/share/download, a profile-scoped attention inbox with complete blocking-request handling, reachable device settings, and selected Cron/memory/skills workflows. Reliable delivery under Android lifecycle constraints and an honest connection/support diagnostic are more important than reproducing desktop file trees or terminal controls. These are product recommendations from the source audit, not implementation claims.

The repository contains focused tests for the active path, including profile controller, workspace browser, conversation UI, connection ownership, notifications, pagination, history search, clarification delivery, intelligence and answer branches. Tests and live integration probes were inventoried but not rerun for this read-only feature audit. See [tests](../../test/profile_workspace_controller_test.dart#L1), [entry-point tests](../../test/workspace_entry_point_test.dart#L1), [profile integration probes](../../integration_test/profile_workspace_test.dart#L1).
