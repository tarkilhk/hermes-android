# Implemented-feature QA sweep

New feature work is paused until the implemented roadmap has been checked,
recorded failures have been fixed and failed checks have passed on retest.
The scope is W00 and the implemented portions of D01-D30 in the
[delivery sequence](DELIVERY_SEQUENCE.md). Deferred features remain deferred.

Use the disposable emulator for routine checks. Production widgets and native
plugins may use isolated gateway data; label that evidence as synthetic.
Use the phone only for a reported live issue, Samsung-specific behavior or final
deployment. No Hermes backend code changes are permitted. Real server mutations
are separate from isolated fixture tests.

## Baseline and execution

- Production baseline: 2.31.1, with 1,288 unit/widget tests passing and four opt-in
  skips. Its analyzer and four emulator scenarios passed before this sweep.
- The sweep reproduced five client defects: Markdown file links, direct-open read
  state, invalid subagent/process snapshots and blank native SVG rendering.
  The signed patch is 2.31.2 / base 2187, ARM64 build 21872. Package identity,
  signature and non-debuggable release status are verified. Installed in place
  on the Samsung phone on 2026-09-13; Android reports 2.31.2 / 21872.
- A fresh baseline run passed all 1,288 tests with four opt-in skips and clean
  analysis. After the fixes, all 83 checks in the affected files and release
  identity checks passed. The full post-fix suite then passed 1,293 tests with
  four opt-in skips. The final run including the native image fix also passed
  1,293 tests in `build/2.31.2-full-tests-final.log`; analysis is clean in
  `build/2.31.2-analyze-verified.log`.
- Earlier native and live evidence remains in
  [emulator verification](EMULATOR_ROADMAP_VERIFICATION.md) and
  [phone acceptance](LIVE_PHONE_ACCEPTANCE_2026-09-12.md).
- Coverage review is in progress across every implemented delivery slice.
  Existing tests alone do not close a missing native or server-contract check.
- All seven expanded emulator scenarios passed in
  `build/2.31.2-emulator-seven-isolated-retry.log`, driver exit 0. No new feature
  work has started. Live-server and Samsung-only acceptance remains open.

## Failure, fix and retest record

| ID | Failure and evidence | Cause or investigation | Fix | Retest |
| --- | --- | --- | --- | --- |
| QA-001 | New vault submission scenario timed out before the request cleared. `build/20260913-emulator-driver.log`. | The test entered text and tapped before the form's enabled state rendered. | Wait for the rendered enabled button before tapping. Test change only. | All four expanded emulator scenarios passed in `build/20260913-qa-emulator-driver.log`, exit 0. |
| QA-002 | After acknowledged vault submission, the new test expected the panel to be removed before the scheduled UI frame rendered. `build/20260913-emulator-driver-final.log`. | Request ID, response value and profile assertions passed before the panel-removal frame. | Wait for rendering after acknowledgement, retaining panel-clearing and secret non-persistence assertions. Test change only. | Same complete emulator pass as QA-001. |
| QA-003 | Markdown preview link never read its target file. Focused regression failed in `build/20260913-qa-links-read-red.log`. | The preview omitted the existing authenticated file-opening callback. | Reuse `_preview` for explicit file links in downloaded Markdown. | Full suite passed. Native link/export passed. Real link tap and two system-Back events passed in `build/2.31.2-nested-link-gesture.log`, with source Markdown asserted between the Back events. |
| QA-004 | Direct opening outside loaded rows sent no read acknowledgement. Focused regression failed in `build/20260913-qa-links-read-red.log`. | Unknown read state was treated as already read. | Acknowledge read after successful history for unknown/unread targets; keep known-read and stale/failure guards. | Focused and full post-fix suites passed, including failed-history no-write regression. |
| QA-005 | Malformed subagent snapshot erased confirmed active work without an error. Regression failed in `build/20260913-qa-focused-round2.log`. | The parser silently dropped invalid rows before replacing the snapshot. | Reject invalid snapshots before changing state; retain previous work and the existing Retry error. Valid sparse/empty lists remain supported. | Focused and full post-fix suites passed, including missing-identity and mixed non-map cases. |
| QA-006 | Malformed process snapshot erased confirmed work without an error. Regression failed in `build/20260913-qa-focused-round2.log`. | Same invalid-snapshot boundary as QA-005. | Preserve confirmed processes on invalid refresh using the existing failure path. | Focused and full post-fix suites passed, including missing-identity and mixed non-map cases. |
| QA-007 | Six-scenario emulator run failed at vault submission, Subagents dismissal and editor dismissal. `build/2.31.2-emulator-six.log`. | Emulator logs show two missed taps during keyboard/scroll movement and an ambiguous text finder. These are test-driver failures. | Wait for keyboard layout, use the scoped Subagents key, reveal the editor close button before tapping, and make missed-tap warnings fatal. | All seven scenarios passed in `build/2.31.2-emulator-seven-isolated-retry.log`. |
| QA-008 | First seven-scenario run failed on the vault button, duplicate steering notice, offscreen diagnostics button and Edit/Resend. `build/2.31.2-emulator-seven.log` and `build/2.31.2-emulator-seven-logcat.log`. | The transcript jump control covered the test's bottom-aligned target; the steering notice also appeared in a snackbar; the test tapped before scrolling settled; RPC fixture history omitted `row_id`. | Center the vault target in its viewport, scope modal dismissal, settle/reveal diagnostics, and return stock RPC row addresses while preserving REST IDs. Test/fixture changes only. | All seven scenarios passed in `build/2.31.2-emulator-seven-isolated-retry.log`. |
| QA-009 | Native SVG stayed blank after opening and recreating the viewer; source was readable. `build/2.31.2-native-svg-visible.png`. | The renderer uses a local blob image; Android's image-blocking setting suppresses its loading. | Permit local image decoding while retaining network blocking, asset interception and existing CSP. | Same native SVG now renders in `build/2.31.2-native-svg-fixed-ready.png`; source switching/return passed. Embedded HTML image and interaction passed. Browser isolation/resource checks passed in `build/2.31.2-diagram-browser-retest.log`. |
| QA-010 | Positive HTML image browser test failed decoding. `build/2.31.2-diagram-browser.log`. | The deliberately permissive CSP inside the test HTML still excluded `data:` images. | Allow data images in that test page's own policy; preserve the production restrictive policy and all blocked external-request assertions. | Complete browser suite passed in `build/2.31.2-diagram-browser-retest.log`. Test fixture only. |
| QA-011 | Second seven-scenario run failed on duplicate Subagents panels and premature Regenerate assertion. `build/2.31.2-emulator-seven-retest.log`. | The same scoped panel exists in the transcript and modal; navigation precedes the child prompt request. | Close the top modal and await the actual acknowledged child operation before asserting its request. | All seven scenarios passed in `build/2.31.2-emulator-seven-isolated-retry.log`. Test changes only. |
| QA-012 | Six scenarios passed, but Goal title was absent in supervision. `build/2.31.2-emulator-seven-final.log`. | The sample goal omitted its required contract. The subsequent expected status label also differed from the production summary. | Include the existing complete goal contract and assert the actual scoped status summary after Pause. | All seven scenarios passed in `build/2.31.2-emulator-seven-isolated-retry.log`. Fixture/test only. |
| QA-013 | Complete goal fixture affected another scenario's scroll gesture and exposed a duplicate Goal menu label. `build/2.31.2-emulator-seven-contract.log`. | Supervision data was enabled for every scenario; a text finder matched both menu and panel. | Enable that sample data only for supervision and target the popup menu items explicitly. | All seven scenarios passed in `build/2.31.2-emulator-seven-isolated-retry.log`. Fixture/test only. |
| QA-014 | Emulator driver lost its service connection before starting scenarios. `build/2.31.2-emulator-seven-isolated.log`. | The emulator process remained alive and paused. | Reconnect the standard driver to the observed current service. No app or test changes. | All seven scenarios passed and driver exited 0 in `build/2.31.2-emulator-seven-isolated-retry.log`. |
| QA-018 | A text file uploaded successfully but its returned reference was rejected as outside the allowed workspace; the response did not prove content delivery. | Current official Desktop comparison found Android incorrectly using file.attach for images, and placing file references after prompt text. Generic file staging outside the workspace remains in the current official server source. See [current source and parity fixes](DESKTOP_ATTACHMENT_REGENERATION_PARITY.md). | 2.31.8 sends images through image.attach_bytes, preserves accepted uploads across retry, detaches removed images, and puts file references before prompt text. No backend change. The original text-file content issue remains open. | 71 focused attachment/draft/queue/regeneration checks pass. A content-dependent live answer is required to close text-file delivery. |
| QA-019 | Attachment-bearing regeneration created a child, then received gateway 4018 while matching the copied saved prompt. | Current Desktop regenerates in the same session. Android inserted an unnecessary branch and targeted its copied row. The precise server text mismatch is still unproven. | 2.31.8 regenerates in place using fresh saved row IDs, matching Desktop. Explicit Branch remains separate. Definite rejections restore the displayed conversation; uncertain submissions are not resent. No backend change. | The old client fails the no-branch regression; 24 answer-action checks pass after the fix. The original attachment-bearing case still needs a live retest. |
| QA-020 | A running default-profile chat appeared again under every other profile in Activity. Opening the false `client-work` row showed the default chat and the original default row disappeared after restart. | `session.active_list` is process-global and has no owner field. Android queried it through every profile gateway and stamped each copy with that gateway's profile. Opening a fabricated row could invoke profile-scoped resume and adopt the session under the wrong profile. | Read the global snapshot once. Show a row only when an exact local runtime and durable ID pair identifies one owner, or when profile-scoped saved metadata identifies one unique owner across every discovered profile. Hide unknown, ambiguous and incompletely checked rows with a coverage notice. Discovery never resumes a session. | Focused and full tests pass. Samsung 2.31.3 showed the running test chat once under its correct profile; tapping returned to its original live conversation. |

## Coverage record

Live retest blocker, QA-028: opening two existing synthetic QA chats returned
"Could not complete that action. Please retry." on both 2.31.7 before installation
and 2.31.8 after installation. The administration checks confirmed authenticated
dashboard access and configured provider credentials. The failure occurs before
attachment submission or regeneration, so neither live result is claimed.
Its exact cause remains unclassified; do not label it a backend defect based on
the generic message. No backend was modified or restarted. Keep this issue ahead
of further feature work and capture the underlying session-open error next.

The additional phone failures are tracked alongside the earlier sweep:

| ID | Failure | Current result |
| --- | --- | --- |
| QA-015 | An unsubmitted chat expired while Camera was foreground; recovered intake could not return to it. | Exact `session.resume` rejection 4007 reproduced. Recovery is limited to a locally created chat with no possible delivered turn. Preserve its profile/project, model/reasoning/YOLO and unsent draft/attachments/queue. Unknown cold targets still require explicit destination choice. Controller regressions pass. Samsung 2.31.4 returned the captured photo to the same unsent chat with its draft and Low reasoning preserved. The photo was removed without sending; the retained text then completed successfully. |
| QA-016 | The expired runtime later produced raw model/file errors and a reconnect banner that outlived the failure. | Samsung 2.31.4 recovery restored a usable composer without a reconnect or read-acknowledgement warning. Sending the retained text completed successfully after removing the unsent test photo. |
| QA-017 | Cold notification launch competed with pending camera review and falsely reported an unavailable chat. | Regression reproduced and fixed by deferring automatic share review when opening a notification. Both routing/share suites pass. Samsung 2.31.3 cold retest passed after verified process removal: the original notification opened its correct chat and latest reply, then Home Review opened the retained camera intake. |
| QA-019 | Attachment-bearing regeneration previously failed with 4018 after branching. | 2.31.8 removes that Android-only branch step and follows current Desktop same-chat rewind. Fresh saved-row validation, draft protection and rejection recovery pass focused tests; live retest pending. |
| QA-021 | Recovery integration made an existing composer widget test hang. | Its draft-write chain retained a completed future across test execution zones. Keep a chain only while writes are pending and clear the settled tail. All 58 composer/recovery tests pass, including typing during a delayed replacement. |
| QA-022 | Camera review still rejected its destination after background recovery had already replaced the expired runtime. | Preserve an in-memory reference from the original capture target to its recovered draft. Resolve it only for explicit draft recovery; ordinary and notification opens remain strict. A regression reproduces background replacement followed by the original capture target. It passes with no second replacement or automatic send. |
| QA-023 | An unsent chat showed a failed read-acknowledgement warning. | Do not acknowledge a known, unsubmitted chat as a saved conversation. Covered by the camera-target regression. |
| QA-024 | New-chat camera return still raised `session.resume` 4007 when it overlapped background replacement. | Two deterministic controller regressions cover replacement beginning before open and during its resume request. Explicit recovery waits for that existing replacement and resolves its validated draft reference. Ordinary notification opens remain strict. The initial regression failed in `build/2.31.4-camera-inflight-red.log`; both cases and all 53 controller checks pass. Clean Samsung 2.31.4 retest passed after time in Camera with the same app process: no destination warning, Add to draft retained the photo/text/Low reasoning, and the text-only follow-up completed after removing the photo. |
| QA-025 | A new unsent chat lost its accessible text draft after Hermes was removed from memory while Camera was open. Capture intake survived, but selecting New chat in review staged only the photo and left the composer empty. | Reproduced on clean Samsung 2.31.4 with verified process removal before capture. Existing draft storage retains the text under the unavailable original session key, but current UI provides no recovery path for that orphaned draft. The cold camera widget regression failed with only incoming content instead of the saved text in `build/2.31.5-cold-camera-red.log`. Review now offers a fresh chat with the recovered draft in the original profile. Atomic transfer preserves text, cached attachments, uncertainty and paused queues; remote attachment references are reset. File-staging retries preserve that recovered draft and reuse its destination. Focused and full checks pass. Clean Samsung 2.31.5 / 21902 passed the same process-removal scenario: recovery review displayed the original text, confirmation restored it with the captured photo, and a text-only send completed after the photo was removed unsent. |
| QA-026 | The composer attachment button was disabled during a running response, preventing preparation of files for the next queued prompt. | Reproduced on Samsung 2.31.5 while a tool-free response streamed. The button and controller blanket-rejected busy chats. Allow next-draft attachments after submission acknowledgement while protecting files during submission and queue mutation. Queue draining waits for file preparation, then resumes through the existing send path; success and failure notify the composer after releasing preparation. The original button regression failed in `build/2.31.6-busy-attachment-red.log`; the completion-during-preparation regression failed in `build/2.31.6-preparation-queue-red.log`. All 86 focused checks and all 1,315 unit/widget tests pass; final analysis is clean. The first Samsung retest opened the menu, but returning from Files during a transient reconnect rejected the file with Wait for the current turn. The focused regression reproduces that exact failure in `build/2.31.6-picker-reconnect-red.log`. Local staging now permits reconnect/settling while the existing submission, replacement, ownership and queue guards remain. All 87 affected checks pass and analysis is clean. Final Samsung 2.31.6 retest passed: the returned file remained staged through the native-picker reconnect while the response continued. A text/file queue entry displayed its filename, waited for completion, drained automatically once, cleared the composer/queue, and received its separate reply in the original chat. QA-018 still prevents claiming successful file-content expansion. |
| QA-027 | A new unsent chat and its text draft had no accessible entry after ordinary app process removal without camera or shared content. | Reproduced on Samsung 2.31.6: enter a synthetic text draft, leave the app, verify process removal, and relaunch. Chats has no draft row. Existing storage retains the record, but cold startup lists only server sessions and cannot discover an unlisted unsent target. The cold-start widget regression failed at the missing Saved drafts section in `build/2.31.7-draft-recovery-red.log`. Chats now lists existing local drafts for its exact connection/profile when no loaded chat row represents them. Opening first resumes the original session; only verified missing-session rejection creates a replacement through the existing atomic recovery path. Double taps cannot create duplicate replacements, uncertainty is retained, and no message is sent automatically. All 89 focused checks and all 1,318 unit/widget tests pass. Samsung 2.31.7 recovered the original inaccessible text; after another verified process removal, exactly one row remained and reopened the same text. The synthetic draft was then cleared unsent. |

### Additional Samsung checks on 2.31.2

The phone was unlocked and interactive testing resumed after installation.
The saved connection remained available. The following results supersede the
earlier device-lock blocker; new failures remain open until repaired and retested.
No backend code or deployment was changed. Per-chat test options were exercised through the existing client.

| Check | Observed result |
| --- | --- |
| Normal camera capture into a saved chat | Capture, confirmation and Add to draft retained the original chat. The staged photo survived force-stop, restart and reopening that chat. Removing it restored an empty draft. The photo was never sent. |
| Camera from an empty new chat, QA-015 | Capture succeeded, but review reported that the original chat could not be reopened. Review and the captured file survived restart. After choosing New chat, later model editing and file submission rejected the retained runtime. A fresh immediately configured new chat worked. Cause remains under investigation; no replacement-session workaround has been accepted. |
| Background capture interruption | Force-stopping Hermes while Camera was reviewing a shot did not recover that photo on manual relaunch. This stronger interruption is inconclusive for ordinary process reclamation; repeat with a verified background process kill before capture. |
| Files picker | A generated 63-byte text fixture was selected and staged successfully. Upload in a saved chat was acknowledged, but content expansion failed; see QA-018. |
| Model and ordinary prompt | Low reasoning applied in a fresh chat and a saved chat. A short tool-free prompt completed; saved reasoning survived reopening. |
| Live completion notification, QA-017 | A warm tap opened the correct chat and latest reply without an error. A cold tap after a verified background process kill reproduced the failure when a pending camera review opened concurrently: target validation found neither the requested scope nor chat selected. Pending content survived. Startup routing fix and cold retest remain open. |
| Saved-message actions, QA-019 | Ordinary Branch and parent navigation passed. Regenerate and Edit/resend passed for a reopened plain-text branch. Regenerating the attachment-bearing prompt failed again after reopening, with gateway rejection 4018. Earlier immediate plain-text Edit failure remains unclassified. Original history remained intact. |
| Text queue | A queued tool-free prompt appeared in the queue, drained after the current response and produced its own reply in the original chat. Attachment queue acceptance remains open. |
| Stop | Stop halted a streaming tool-free response and restored the idle composer. The partial response remained visible. |
| Slash command during a response | `/yolo` returned "YOLO enabled for this session" while the response was still streaming. The response completed normally. A second call confirmed the mode was disabled again. |

QA-016 records the related stale reconnect banner and raw model/file errors
after the empty-chat recovery failure. The banner remained visible during a
later successful new-chat prompt. Private transcripts, addresses and local
camera contents are excluded from this record.

The automated evidence below describes existing coverage. The baseline and fresh
post-fix full suite both passed. Native/live
observations are dated in the linked evidence records. A gap is not automatically
a defect, and a fixture pass is not live-server verification.

| Slice and implemented behavior | Automated coverage | Native/live evidence and remaining check |
| --- | --- | --- |
| W00 shell, drawer and device settings | `app_shell_navigation_test.dart`; emulator navigation and large text | Native emulator drawer/settings passed. |
| D01 text and attachment drafts, restart, missing-file handling | Draft store, attachment draft, shared draft and workspace controller tests | Samsung text restart and emulator attachment restart passed. Samsung 2.31.7 ordinary cold-start discovery/recovery of an unlisted unsent chat passed twice, including recovery of the original failed-case draft. Missing staged-file recovery remains automated evidence. |
| D02 reconnect, server refresh, uncertain-send protection | Workspace controller, gateway turn recovery and recovery fallback tests | Existing-chat reopen passed on Samsung. Actual pending clarification after process death and lost-ack duplicate prevention need deployed-contract acceptance. |
| D03 provider/model/reasoning selection | Intelligence and picker tests; emulator route/model writes | Samsung provider grouping and per-chat reasoning passed. External config changes followed by reopen need cross-client acceptance. |
| D04 session YOLO and slash routing | Slash command tests; emulator advertised command dispatch | Samsung `/yolo` acknowledgement during a streaming response passed; mode was subsequently disabled. |
| D05 cross-profile Activity and filters | Live Activity, gateway Activity and workspace filter tests; emulator owned navigation | QA-020 found that the deployed active list is global and unowned. Client containment is covered by focused tests. Samsung 2.31.3 showed one correctly owned running row and opened its original chat. Input-required rows remain unverified. |
| D06 approvals and multi-question clarification | Approval, clarification, clarification-screen and request-replacement tests; emulator Allow once | Real advertised scopes and multi-question response acknowledgement remain unverified. |
| D07 sudo/secret/vault forms, expiry and non-persistence | Sensitive prompt and delivery tests; expanded emulator vault response | Native vault submission, exact scope/acknowledgement and no secret in drafts/history/preferences passed. Live response delivery unverified; cold recovery is deferred. |
| D08 local alerts, settings and target routing | Notification service, sink, settings and routing tests | Emulator warm/cold scoped taps and Samsung test alert passed. Samsung 2.31.3 warm/cold real chat alert routing passed, including retained pending camera intake on cold start. |
| D09 text/file queues, order, removal, uncertainty and one-shot Steer | Composer queue and controller tests; emulator text queue ownership/drain | Samsung text and attachment queue ownership and automatic drain passed on 2.31.6, including native-picker interruption before queuing. Upload acknowledgement and a separate reply passed; QA-018 file-content expansion remains external. Samsung one-shot Steer was acknowledged, produced the requested separate reply and cleared its text without changing normal Send. Lost-ack attachment interruption remains unverified. |
| D10 Edit/Resend, Regenerate, Branch and Fork | Saved-message action and ordinary answer-action tests | Emulator real controls passed with exact scoped requests, retained unrelated draft, parent navigation and one-shot Fork. Deployed-contract acceptance remains open. Synchronized versions are deferred. |
| D11 live side/background questions, results and review notices | Side-question, question-delivery and review-notice tests; emulator review card | Live event acceptance remains open; cold snapshots are deferred. |
| D12 tables, code, Mermaid and SVG | Phone Markdown and diagram tests, including large text | Native wide table scrolling, code wrap/unwrapping, Mermaid and corrected SVG rendering/source/return passed. Streamed layout remains covered by widget tests; native zoom remains unverified. |
| D13 image/media/web content | Image preview, media, web, output and output-screen tests | Emulator audio/video/browser passed. Samsung playback and linked media remain open. |
| D14 tools, arguments/results, timing, todos and reasoning | Execution activity tests | Native tool expansion, arguments/results, 400 ms duration, two task states and reasoning disclosure passed with local data. Real event acceptance remains open. |
| D15 context fuse, estimates and reopen races | Context fuse and context usage tests | Reported Samsung reopen regression passed with immediate 57% fullness. Other live providers unverified. |
| D16 per-chat Outputs, older pages, retries and downloads | Output extraction, output screen, large Outputs and remote files tests | Samsung large-chat pagination passed. Native Android share sheet received the named linked text file; no external recipient selected. Old 404 paths remain unresolved. |
| D17 PDF, Markdown, text, HTML and media viewers | Output screen, PDF, web and media tests | Fresh native PDF next/previous, audio/video preparation and advancing/paused timelines, visible MP4 frame, embedded HTML image and interaction passed. Linked text opens; real-tap/system-Back regression passed. Earlier native browser check passed. |
| D18 Find, older matches, nearby navigation and return | Find, history search, pagination and large Outputs tests | Reported Samsung Find flow passed, including older batches and return to latest. |
| D19 unread state, list filters and ownership | Unread filter, row action and Activity filter tests | Cross-client read state remains open. QA-004 investigates direct opening outside loaded lists. |
| D20 project create/rename/appearance/delete, folders and membership | Project gateway, management, action, discovery and folder-picker tests | Synthetic create/rename/cancel-delete/delete passed before vault-test failure. Live discovery passed; real project writes remain unverified. |

| Slice and implemented behavior | Automated coverage | Native/live evidence and remaining check |
| --- | --- | --- |
| D21 reviewed Android share, routing, merge, acknowledgement and discard | Share bridge, platform contract, Home review, shared draft, review and attachment-preparer tests | Emulator share/merge/restart and Samsung text review/discard restart passed. Samsung My Files shared both the named text fixture and PNG through Hermes Personal; review preserved each filename and staged it into the selected existing chat. Both were removed unsent. |
| D22 camera/photo/files and captured destination recovery | Camera action plus share/review/preparer tests | Emulator capture, interrupted capture, Files and Photos passed. Samsung saved-chat capture, review, draft staging and restart passed. Samsung 2.31.4 new unsent-chat capture/recovery and subsequent text send passed. Saved-chat capture after verified background process removal passed on 2.31.4. New-chat capture after process removal passed through explicit draft recovery on 2.31.5. Samsung Photos selection through Gallery staged the exact test PNG and returned to the original chat; it was removed unsent. |
| D23 subagent roster, live tail, Steer and Interrupt | Subagent controller and panel tests, including stale reads and acknowledgement races | Emulator roster, tail and acknowledged Steer passed with the exact child/chat/profile. Interrupt has widget/controller coverage. Live child-control acceptance remains open. |
| D24 goals, criteria, pause/resume/unwait/clear | Session control, profile session control and goal panel tests | Emulator goal details and acknowledged Pause passed. Other actions have widget/controller coverage. Live controls remain open; contracts and gates are read-only by scope. |
| D25 heartbeat, loop and process Stop/Dismiss | Gateway process, background work controller and panel tests | Emulator heartbeat display, Loop Pause, targeted process Stop and local Dismiss/no-kill passed. Live acceptance remains open. |
| D26-D27 local notifications and optional receiver | Notification service, settings, routing and background-push tests | Native warm/cold routing and Samsung local test passed. Dormant push fixture tests do not establish a server registration/sender contract; FCM remains deferred. |
| D28 connections, credentials, proxy headers, diagnostics and usage | Connection restore/manager/storage, header editor/transport, diagnostics and usage tests | Saved live connection and ordinary data access passed. Emulator scoped usage, grouped numbers and partial diagnostics passed. Live proxy variants remain open. |
| D29 selected-profile description/SOUL editor | Profile editor and administration navigation tests, including dirty fields, partial acknowledgement and dismissal guard | Emulator Cancel/no-write, partial save and retained SOUL draft passed. Live save/restore remains open. Broad K administration remains deferred. |
| D30 app/backend versions, release links and update controls | App version, backend version/update controller/screen and release identity tests | Emulator and Samsung app identity passed. Emulator update Cancel/no-write and eligibility refusal passed. Live eligibility/status remains open. No actual server update was performed. |

## Execution queue

Patch 2.31.7 passes all 1,318 unit/widget tests with four opt-in skips in `build/2.31.7-full-tests.log`, all 89 focused draft/queue checks, and clean analysis. The signed 2.31.7 / 21922 package is installed on Samsung. The original ordinary-restart draft failure and a second process-removal cycle both passed; file/image sharing and Photos selection also passed. All seven emulator scenarios completed in `build/2.31.7-emulator-scenarios.log`, and the standard driver exited 0 in `build/2.31.7-emulator-final-driver.log`. Launch without pausing Dart avoided the earlier focus timeout; Android recorded no new ANR during this run. Initial driver attempts lost their local port forward. Restoring that forward allowed both getVM/getIsolate and the standard driver to complete without changing app tests or SDK code.

Patch 2.31.6 passes the full 1,315-test baseline with four opt-in skips, then all 87 affected checks after the final local-staging reconnect correction. Analysis is clean in `build/2.31.6-analyze-picker.log`. The signed 2.31.6 / 21912 build passed native attachment preparation, picker-return reconnect and automatic text/file queue draining on Samsung. The seven emulator scenarios and standard driver passed before the final reconnect guard correction in `build/2.31.6-emulator-verified.log` and `build/2.31.6-emulator-scenarios.log`. The initial ADB launch disconnected; the app later displayed a focus-timeout dialog during the debug run. That dialog was dismissed after the tests, so this run is not evidence of a clean native launch. Retest launch on the next patch.

Patch 2.31.5 passes all 1,312 unit/widget tests with four opt-in skips in `build/2.31.5-full-tests.log`. The five-file focused set passes 84 checks, and all seven shared-review checks pass with the added recovery retry case. A braces-only analyzer correction followed the full run; final analysis is clean in `build/2.31.5-analyze.log`. The signed 2.31.5 / 21902 release was installed and its cold camera draft recovery passed on Samsung, followed by a successful text-only send. No test photo was sent.

Patch 2.31.4 passes all 1,307 unit/widget tests with four opt-in skips in `build/2.31.4-full-tests.log`. Analysis is clean in `build/2.31.4-analyze.log`. The signed release was verified and installed as 2.31.4 / 21892. The same-process new-chat camera recovery and subsequent text-only send passed on Samsung. The test photo was removed unsent. Saved-chat capture after verified background process removal passed on Samsung: the app relaunched, review retained the photo and original destination, and Add to draft succeeded. The photo was removed unsent. New unsent-chat process removal exposed QA-025 below.

Patch 2.31.3 adds client fixes from the live sweep. All 1,305 unit/widget tests
pass with four opt-in skips in `build/2.31.3-full-tests-camera-final.log`; static
analysis is clean in `build/2.31.3-analyze-camera-final.log`. The normal composer and
recovery suites also pass all 58 checks separately. The signed 2.31.3 / 21882 release was installed successfully. All seven emulator scenarios passed in `build/2.31.3-emulator-verified.log`. Samsung cold notification routing and live Activity ownership passed. Its remaining camera/reconnect race was subsequently fixed and retested in 2.31.4 above.

1. Completed: five confirmed client defects fixed and retested; all 1,293
   unit/widget checks and seven emulator scenarios pass; browser renderer checks
   pass and analysis is clean. Native coverage and remaining limits are above.
2. The signed 2.31.2 package is built and verified in
   `build/2.31.2-release-build.log`. Installation over the existing Samsung app
   succeeded on 2026-09-13, and Android reports version 2.31.2 / 21872.
   The initial device lock was cleared, and interactive acceptance resumed.
   Additional failures and observed passes are recorded above.
3. Complete the remaining live-server and Samsung-only acceptance. The coverage
   table deliberately keeps these open. No real backend update or project write
   was performed in this emulator sweep.
4. Keep new feature work paused until those remaining checks are completed or
   explicitly deferred. Passing available tests is not a claim that every device
   or deployed-server condition has been verified.

Live project-write acceptance remains pending. Automatic approval review
rejected creating a temporary server project under the no-backend-modifications
constraint. The isolated emulator project checks do not bypass that rejection;
the clarification request for real server writes remains unanswered.

## Resolution plan for QA-018 and QA-019

Update for 2.31.8: the [current official Desktop comparison](DESKTOP_ATTACHMENT_REGENERATION_PARITY.md)
found and corrected Android differences in image upload and regeneration.
The isolated current-source file staging/path check reproduces QA-018, with an
inside-workspace file passing the control. The full suite passed 1,324 tests
with four opt-in skips and one stale release-version assertion. After updating
release metadata, all three release identity checks pass, for 1,325 passing
tests overall. The 71 focused checks also pass. Final static analysis is clean.
Signed Personal 2.31.8 / 21932 installed successfully and launched on the phone.
The existing emulator answer-action scenario now asserts same-chat regeneration;
it was updated and analyzed but not rerun in this pass. Live attachment and
regeneration checks remain pending because of QA-028 above.

These failures are separate. QA-018 has a confirmed server path rejection.
QA-019 has a confirmed gateway consistency rejection, but its exact cause and
the component requiring a correction are not established. Neither permits
unrequested backend code, configuration or deployment changes.

- QA-018: use one synthetic text file with a value absent from the prompt.
  Compare Android and Desktop upload/submission requests against the same
  gateway and session workspace. Check whether the existing API provides a
  supported attachment route that avoids the rejected path. If so, correct
  Android's request and verify a content-dependent answer. If the gateway
  rejects its own returned reference for both clients and provides no usable
  alternative, record an upstream reproducer. The server remedy would need to
  make its staged attachments readable to the intended session while retaining
  filesystem access restrictions. Such a remedy requires separate authorization;
  do not invent paths, paste file contents into prompts, or weaken path checks.
- QA-019: retest same-chat regeneration of the attachment-bearing prompt using
  its fresh source row, matching current Desktop. Explicit Branch remains a
  separate action. The existing
  literal attachment-warning database round trip did not reproduce the failure,
  so do not assume that warning text causes it. Correct Android only if its
  request or sequencing differs from the supported contract. If the server's
  branch/history/submission methods disagree with one another, retain a minimal
  upstream reproducer and the existing actionable client error. Never substitute
  an unchecked message position or disable the gateway's consistency guard.

Verification is limited to the failing flow and a plain-text control. QA-018
passes only when the answer proves the attachment contents were available.
QA-019 passes only when regeneration targets the intended prompt, creates one
replacement response in the same chat, and retains earlier history and unsent
drafts. The selected answer and subsequent messages are intentionally replaced,
as on Desktop. Broader testing
waits for an actual client change; repeated full-suite or phone rebuilds are not
part of diagnosis. Fixing QA-018 does not establish that QA-019 is fixed.

## Next feature work

Complete this QA sweep before building more features. The agreed administration
scope is the small D28-D30 client flow already implemented: connection diagnostics,
usage, versions/update controls and profile description/SOUL editing. Broader
provider, tool, skill, plugin and memory administration remains deferred in the
product plan. This sweep does not authorize expanding it.
