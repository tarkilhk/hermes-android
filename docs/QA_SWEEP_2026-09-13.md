# Implemented-feature QA sweep

New feature work is paused until the implemented roadmap has been checked,
recorded failures have been fixed and failed checks have passed on retest.
The scope is W00 and the implemented portions of D01-D30 in the
[delivery sequence](DELIVERY_SEQUENCE.md). Deferred features remain deferred.

Use the disposable emulator for routine checks. Production widgets and native
plugins may use isolated gateway data; label that evidence as synthetic.
Use the phone only for a reported live issue, Samsung-specific behavior or final
deployment. No Hermes backend code changes are permitted. Real server mutations
are separate from isolated fixture tests.

## Baseline and execution

- Production baseline: 2.31.1, with 1,288 unit/widget tests passing and four opt-in
  skips. Its analyzer and four emulator scenarios passed before this sweep.
- The sweep reproduced five client defects: Markdown file links, direct-open read
  state, invalid subagent/process snapshots and blank native SVG rendering.
  The candidate patch version is 2.31.2 / 2187. It is not yet installed.
- A fresh baseline run passed all 1,288 tests with four opt-in skips and clean
  analysis. After the fixes, all 83 checks in the affected files and release
  identity checks passed. The full post-fix suite then passed 1,293 tests with
  four opt-in skips. The final run including the native image fix also passed
  1,293 tests in `build/2.31.2-full-tests-final.log`; analysis is clean in
  `build/2.31.2-analyze-verified.log`.
- Earlier native and live evidence remains in
  [emulator verification](EMULATOR_ROADMAP_VERIFICATION.md) and
  [phone acceptance](LIVE_PHONE_ACCEPTANCE_2026-09-12.md).
- Coverage review is in progress across every implemented delivery slice.
  Existing tests alone do not close a missing native or server-contract check.
- The latest emulator run passed six of seven scenarios. The remaining
  supervision scenario needs its corrected goal fixture retested. No new feature
  work has started.

## Failure, fix and retest record

| ID | Failure and evidence | Cause or investigation | Fix | Retest |
| --- | --- | --- | --- | --- |
| QA-001 | New vault submission scenario timed out before the request cleared. `build/20260913-emulator-driver.log`. | The test entered text and tapped before the form's enabled state rendered. | Wait for the rendered enabled button before tapping. Test change only. | All four expanded emulator scenarios passed in `build/20260913-qa-emulator-driver.log`, exit 0. |
| QA-002 | After acknowledged vault submission, the new test expected the panel to be removed before the scheduled UI frame rendered. `build/20260913-emulator-driver-final.log`. | Request ID, response value and profile assertions passed before the panel-removal frame. | Wait for rendering after acknowledgement, retaining panel-clearing and secret non-persistence assertions. Test change only. | Same complete emulator pass as QA-001. |
| QA-003 | Markdown preview link never read its target file. Focused regression failed in `build/20260913-qa-links-read-red.log`. | The preview omitted the existing authenticated file-opening callback. | Reuse `_preview` for explicit file links in downloaded Markdown. | Full suite passed. Native link/export passed. Real link tap and two system-Back events passed in `build/2.31.2-nested-link-gesture.log`, with source Markdown asserted between the Back events. |
| QA-004 | Direct opening outside loaded rows sent no read acknowledgement. Focused regression failed in `build/20260913-qa-links-read-red.log`. | Unknown read state was treated as already read. | Acknowledge read after successful history for unknown/unread targets; keep known-read and stale/failure guards. | Focused and full post-fix suites passed, including failed-history no-write regression. |
| QA-005 | Malformed subagent snapshot erased confirmed active work without an error. Regression failed in `build/20260913-qa-focused-round2.log`. | The parser silently dropped invalid rows before replacing the snapshot. | Reject invalid snapshots before changing state; retain previous work and the existing Retry error. Valid sparse/empty lists remain supported. | Focused and full post-fix suites passed, including missing-identity and mixed non-map cases. |
| QA-006 | Malformed process snapshot erased confirmed work without an error. Regression failed in `build/20260913-qa-focused-round2.log`. | Same invalid-snapshot boundary as QA-005. | Preserve confirmed processes on invalid refresh using the existing failure path. | Focused and full post-fix suites passed, including missing-identity and mixed non-map cases. |
| QA-007 | Six-scenario emulator run failed at vault submission, Subagents dismissal and editor dismissal. `build/2.31.2-emulator-six.log`. | Emulator logs show two missed taps during keyboard/scroll movement and an ambiguous text finder. These are test-driver failures. | Wait for keyboard layout, use the scoped Subagents key, reveal the editor close button before tapping, and make missed-tap warnings fatal. | Seven-scenario rerun pending. |
| QA-008 | First seven-scenario run failed on the vault button, duplicate steering notice, offscreen diagnostics button and Edit/Resend. `build/2.31.2-emulator-seven.log` and `build/2.31.2-emulator-seven-logcat.log`. | The transcript jump control covered the test's bottom-aligned target; the steering notice also appeared in a snackbar; the test tapped before scrolling settled; RPC fixture history omitted `row_id`. | Center the vault target in its viewport, scope modal dismissal, settle/reveal diagnostics, and return stock RPC row addresses while preserving REST IDs. Test/fixture changes only. | Corrected seven-scenario rerun pending. |
| QA-009 | Native SVG stayed blank after opening and recreating the viewer; source was readable. `build/2.31.2-native-svg-visible.png`. | The renderer uses a local blob image; Android's image-blocking setting suppresses its loading. | Permit local image decoding while retaining network blocking, asset interception and existing CSP. | Same native SVG now renders in `build/2.31.2-native-svg-fixed-ready.png`; source switching/return passed. Embedded HTML image and interaction passed. Browser isolation/resource checks passed in `build/2.31.2-diagram-browser-retest.log`. |
| QA-010 | Positive HTML image browser test failed decoding. `build/2.31.2-diagram-browser.log`. | The deliberately permissive CSP inside the test HTML still excluded `data:` images. | Allow data images in that test page's own policy; preserve the production restrictive policy and all blocked external-request assertions. | Complete browser suite passed in `build/2.31.2-diagram-browser-retest.log`. Test fixture only. |
| QA-011 | Second seven-scenario run failed on duplicate Subagents panels and premature Regenerate assertion. `build/2.31.2-emulator-seven-retest.log`. | The same scoped panel exists in the transcript and modal; navigation precedes the child prompt request. | Close the top modal and await the actual acknowledged child operation before asserting its request. | Final seven-scenario run pending. Test changes only. |
| QA-012 | Six scenarios passed, but Goal title was absent in supervision. `build/2.31.2-emulator-seven-final.log`. | The sample goal omitted its required contract. The subsequent expected status label also differed from the production summary. | Include the existing complete goal contract and assert the actual scoped status summary after Pause. | Emulator rerun pending. Fixture/test only. |

## Coverage record

The automated evidence below describes existing coverage. The baseline and fresh
post-fix full suite both passed. Native/live
observations are dated in the linked evidence records. A gap is not automatically
a defect, and a fixture pass is not live-server verification.

| Slice and implemented behavior | Automated coverage | Native/live evidence and remaining check |
| --- | --- | --- |
| W00 shell, drawer and device settings | `app_shell_navigation_test.dart`; emulator navigation and large text | Native emulator drawer/settings passed. |
| D01 text and attachment drafts, restart, missing-file handling | Draft store, attachment draft, shared draft and workspace controller tests | Samsung text restart and emulator attachment restart passed. Missing staged-file recovery remains automated evidence. |
| D02 reconnect, server refresh, uncertain-send protection | Workspace controller, gateway turn recovery and recovery fallback tests | Existing-chat reopen passed on Samsung. Actual pending clarification after process death and lost-ack duplicate prevention need deployed-contract acceptance. |
| D03 provider/model/reasoning selection | Intelligence and picker tests; emulator route/model writes | Samsung provider grouping and per-chat reasoning passed. External config changes followed by reopen need cross-client acceptance. |
| D04 session YOLO and slash routing | Slash command tests; emulator advertised command dispatch | Current deployed `/yolo` while running remains unverified. |
| D05 cross-profile Activity and filters | Live Activity, gateway Activity and workspace filter tests; emulator owned navigation | Samsung empty result passed; real running/input-required rows remain unverified. |
| D06 approvals and multi-question clarification | Approval, clarification, clarification-screen and request-replacement tests; emulator Allow once | Real advertised scopes and multi-question response acknowledgement remain unverified. |
| D07 sudo/secret/vault forms, expiry and non-persistence | Sensitive prompt and delivery tests; expanded emulator vault response | Native vault submission, exact scope/acknowledgement and no secret in drafts/history/preferences passed. Live response delivery unverified; cold recovery is deferred. |
| D08 local alerts, settings and target routing | Notification service, sink, settings and routing tests | Emulator warm/cold scoped taps and Samsung test alert passed. Real Samsung chat alert remains unverified. |
| D09 text/file queues, order, removal, uncertainty and one-shot Steer | Composer queue and controller tests; emulator text queue ownership/drain | Live attachment upload/drain/interruption and Steer acknowledgement remain unverified. |
| D10 Edit/Resend, Regenerate, Branch and Fork | Saved-message action and ordinary answer-action tests | Emulator real controls passed with exact scoped requests, retained unrelated draft, parent navigation and one-shot Fork. Deployed-contract acceptance remains open. Synchronized versions are deferred. |
| D11 live side/background questions, results and review notices | Side-question, question-delivery and review-notice tests; emulator review card | Live event acceptance remains open; cold snapshots are deferred. |
| D12 tables, code, Mermaid and SVG | Phone Markdown and diagram tests, including large text | Native wide table scrolling, code wrap/unwrapping, Mermaid and corrected SVG rendering/source/return passed. Streamed layout remains covered by widget tests; native zoom remains unverified. |
| D13 image/media/web content | Image preview, media, web, output and output-screen tests | Emulator audio/video/browser passed. Samsung playback and linked media remain open. |
| D14 tools, arguments/results, timing, todos and reasoning | Execution activity tests | Native tool expansion, arguments/results, 400 ms duration, two task states and reasoning disclosure passed with local data. Real event acceptance remains open. |
| D15 context fuse, estimates and reopen races | Context fuse and context usage tests | Reported Samsung reopen regression passed with immediate 57% fullness. Other live providers unverified. |
| D16 per-chat Outputs, older pages, retries and downloads | Output extraction, output screen, large Outputs and remote files tests | Samsung large-chat pagination passed. Native Android share sheet received the named linked text file; no external recipient selected. Old 404 paths remain unresolved. |
| D17 PDF, Markdown, text, HTML and media viewers | Output screen, PDF, web and media tests | Fresh native PDF next/previous, audio/video preparation and advancing/paused timelines, visible MP4 frame, embedded HTML image and interaction passed. Linked text opens; real-tap/system-Back regression passed. Earlier native browser check passed. |
| D18 Find, older matches, nearby navigation and return | Find, history search, pagination and large Outputs tests | Reported Samsung Find flow passed, including older batches and return to latest. |
| D19 unread state, list filters and ownership | Unread filter, row action and Activity filter tests | Cross-client read state remains open. QA-004 investigates direct opening outside loaded lists. |
| D20 project create/rename/appearance/delete, folders and membership | Project gateway, management, action, discovery and folder-picker tests | Synthetic create/rename/cancel-delete/delete passed before vault-test failure. Live discovery passed; real project writes remain unverified. |

| Slice and implemented behavior | Automated coverage | Native/live evidence and remaining check |
| --- | --- | --- |
| D21 reviewed Android share, routing, merge, acknowledgement and discard | Share bridge, platform contract, Home review, shared draft, review and attachment-preparer tests | Emulator share/merge/restart and Samsung text review/discard restart passed. Third-party sender selection and Samsung file/image success remain open. |
| D22 camera/photo/files and captured destination recovery | Camera action plus share/review/preparer tests | Emulator capture, interrupted capture, Files and Photos passed. Samsung cancellation passed; successful capture and recovery remain open. |
| D23 subagent roster, live tail, Steer and Interrupt | Subagent controller and panel tests, including stale reads and acknowledgement races | No live child-control acceptance. QA-005 checks invalid snapshots; emulator control interaction remains open. |
| D24 goals, criteria, pause/resume/unwait/clear | Session control, profile session control and goal panel tests | Native/live control acceptance remains open; contracts and gates are read-only by scope. |
| D25 heartbeat, loop and process Stop/Dismiss | Gateway process, background work controller and panel tests | Native/live control acceptance remains open. QA-006 checks invalid process snapshots. |
| D26-D27 local notifications and optional receiver | Notification service, settings, routing and background-push tests | Native warm/cold routing and Samsung local test passed. Dormant push fixture tests do not establish a server registration/sender contract; FCM remains deferred. |
| D28 connections, credentials, proxy headers, diagnostics and usage | Connection restore/manager/storage, header editor/transport, diagnostics and usage tests | Saved live connection and ordinary data access passed. Emulator scoped usage, grouped numbers and partial diagnostics passed. Live proxy variants remain open. |
| D29 selected-profile description/SOUL editor | Profile editor and administration navigation tests, including dirty fields, partial acknowledgement and dismissal guard | Emulator Cancel/no-write, partial save and retained SOUL draft passed. Live save/restore remains open. Broad K administration remains deferred. |
| D30 app/backend versions, release links and update controls | App version, backend version/update controller/screen and release identity tests | Emulator and Samsung app identity passed. Emulator update Cancel/no-write and eligibility refusal passed. Live eligibility/status remains open. No actual server update was performed. |

## Execution queue

1. Finish the expanded emulator scenario retest and the fresh full automated run.
2. Reproduce QA-003 through QA-006, fix confirmed failures and rerun affected tests.
3. Exercise remaining client controls on the emulator with supported synthetic
   responses: saved-message actions; subagents/goals/heartbeat/processes;
   diagnostics/usage/profile editor/update refusal and partial failure.
4. Run the full suite, analyzer and emulator scenarios against the final fixes.
5. Record live-server and Samsung-only checks still unavailable separately.
   Do not start new feature work while those are silently treated as passed.

## Next feature work

Complete this QA sweep before building more features. The agreed administration
scope is the small D28-D30 client flow already implemented: connection diagnostics,
usage, versions/update controls and profile description/SOUL editing. Broader
provider, tool, skill, plugin and memory administration remains deferred in the
product plan. This sweep does not authorize expanding it.
